language: objective-c
osx_image: xcode7.2b1
xcode_project: SwiftMongoDB.xcodeproj
xcode_scheme: SwiftMongoDB-OSX
before_install:
    - curl -OlL "https://github.com/Carthage/Carthage/releases/download/0.10/Carthage.pkg"
    - sudo installer -pkg "Carthage.pkg" -target /
    - rm "Carthage.pkg"
install:
    - carthage bootstrap --no-build
script:
    - set -o pipefail && xcodebuild -verbose -project SwiftMongoDB.xcodeproj -scheme SwiftMongoDB-OSX -sdk macosx build CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | xcpretty -c